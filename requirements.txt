# Core Flask dependencies
Flask==2.3.3
flask-cors==4.0.0

# Data processing and analysis
pandas==2.0.3
numpy==1.24.3
scipy==1.11.4
geopandas==0.13.2
shapely==2.0.1

# Production components (NEW)
redis==4.6.0                # Redis client for caching
APScheduler==3.10.4         # Background job scheduling
python-dotenv==1.0.0        # Environment variable management

# Production servers
gunicorn==21.2.0  # For Linux/Unix
waitress==2.1.2   # For Windows

# Performance and optimization
ujson==5.8.0                # Faster JSON parsing
scikit-learn==1.3.2         # For advanced heatmap generation

# Spatial indexing and performance
rtree==1.0.1                # Spatial indexing (requires GEOS)
pygeos==0.14.0              # Alternative spatial operations

# HTTP client for Metabase API
requests==2.31.0            # HTTP client (already used in mini.py)

# Phase 2: Database support
SQLAlchemy==2.0.23          # Database ORM
psycopg2-binary==2.9.9      # PostgreSQL adapter  
alembic==1.13.1             # Database migrations

# Phase 2: Background task processing
celery==5.3.4               # Distributed task queue
kombu==5.3.4                # Message passing library

# Phase 2: Enhanced data processing
asyncio-throttle==1.0.2     # Rate limiting for async operations

# Optional: Monitoring and observability (for Phase 3)  
# prometheus-client==0.19.0 # Metrics collection
# structlog==23.2.0         # Structured logging
 